"""
Централизованная конфигурация парсера CyberLeninka.

Все настройки вынесены сюда, чтобы main.py и parser.py
не содержали «магических» констант.
"""

from pathlib import Path
from typing import List, Optional

# -------------------------------------------------------
# Темы поиска
# -------------------------------------------------------
TOPICS: List[str] = [
    "Философия, этика, религиоведение",
    "История и археология",
    "Математика",
    "Право",
    "Политологические науки",
]

# -------------------------------------------------------
# Параметры выгрузки
# -------------------------------------------------------

# Кол-во страниц API на одну тему.
# Итого статей = MAX_PAGES × ARTICLES_PER_PAGE
# Например: 60 × 10 = 600 статей/тему
MAX_PAGES: int = 60

# Размер одной страницы API (фиксирован CyberLeninka).
# Менять не нужно.
ARTICLES_PER_PAGE: int = 10

# Фильтр по базе индексации:
#   None — без фильтра
#   22   — РИНЦ
#   8    — ВАК
#   2    — Scopus
FILTER: Optional[int] = None

# Задержка между HTTP-запросами в секундах.
# Увеличьте если сервер возвращает 429 Too Many Requests.
DELAY: float = 1.2

# Пытаться ли скачать PDF и извлечь полный текст?
#   False — только HTML-страница (~1-2 с/статья)
#   True  — HTML + PDF при необходимости (~5-15 с/статья)
FETCH_PDF_TEXT: bool = False

# -------------------------------------------------------
# URL / заголовки
# -------------------------------------------------------
API_URL           = "https://cyberleninka.ru/api/search"
BASE_URL          = "https://cyberleninka.ru"

HEADERS = {
    "User-Agent": (
        "Mozilla/5.0 (Windows NT 10.0; Win64; x64) "
        "AppleWebKit/537.36 (KHTML, like Gecko) "
        "Chrome/120.0.0.0 Safari/537.36"
    ),
    "Content-Type": "application/json",
    "Referer": "https://cyberleninka.ru/",
}

# -------------------------------------------------------
# Выходная директория и именование файлов
# -------------------------------------------------------

# Общая папка data в корне репозитория (рядом с newsapi, lenta и т.д.).
_DATA_ROOT = Path(__file__).resolve().parent.parent.parent
DATA_DIR = _DATA_ROOT / "data"

# Базовое имя файла (без даты и расширения).
# К нему добавляется метка времени: articles_20260307_143022.csv
FILE_BASENAME = "cyberleninka_articles"
